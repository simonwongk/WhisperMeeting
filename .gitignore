.build/
.DS_Store
*.xcuserstate

# Local AI-tool state and generated work reports
.claude/
.superpowers/
# AGENTS.md is the single agent guide; do not restore the retired pointer.
CLAUDE.md

# Python bytecode
__pycache__/
*.pyc

# Ticketing board + generated dashboard + generator — local-only agent work state (user request 2026-08-07)
docs/tickets-dashboard.html
docs/TICKETS.md
docs/TICKET_LOG.md
Scripts/generate-tickets-dashboard.py
Scripts/tests/test_generate_tickets_dashboard.py

# Generated diarization fixtures — regenerable from manifest.json, verified by manifest.lock.json
Scripts/bench/diarization/out/
Scripts/bench/diarization/.cache/

# Content-fidelity bench (F244) — corpus, results and the sensitive appendix are local-only
# (user decision 2026-09-14; the GitHub remote is public). Harness code and neutral fixtures are tracked.
Scripts/bench/fidelity/corpus/
Scripts/bench/fidelity/results/

# F241: the long-form bench fixture is generated from the committed clips, never committed —
# 16 MB of derived PCM, reproducible by `Scripts/bench/longform/make-longform-fixture.py`.
Scripts/bench/longform/build/
